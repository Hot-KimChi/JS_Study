const calculator = {
    plus: function (a, b) {
        return a + b;
    },
    minus: function (a, b) {
        return a - b;
    },
    multiple: function (a, b) {
        return a * b;
    }
}

const plusResult = calculator.plus(2, 3)
console.log(typeof plusResult)

parseInt(plusResult)
 console.log(typeof plusResult)
